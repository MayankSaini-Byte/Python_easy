# ⏰ Python CLI Timer Alarm

A simple **command-line based timer alarm** built using Python.  
The program allows users to set a timer in minutes and seconds, displays a live countdown, and plays an alarm sound when the timer ends.

This project focuses on **Python fundamentals**, clean structure, and basic error handling.

---

## 🚀 Features
- Set timer using minutes and seconds
- Live countdown display (MM:SS)
- Plays alarm sound when time is up
- Handles invalid user input gracefully
- Clean function-based structure
- Uses `__main__` guard for safe execution

---

## 🛠️ Technologies Used
- Python 3.x
- `time` module
- `playsound` library (audio playback)

---
